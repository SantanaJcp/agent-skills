{
  "acta2": "0.2.0-pilot",
  "kind": "model",
  "decisionId": "D-01",
  "acceptanceToken": "model-accepted",
  "skill": "concept-lab",
  "artifactKind": "concept-model",
  "initiative": "[confirmed-initiative-slug]",
  "title": "Concept lab",
  "question": "[The one conceptual question this model lets the human feel]",
  "sourceRevision": null,
  "inputs": ["[trusted source — author, title, year]", "[second trusted source]"],
  "unresolved": ["[what this model deliberately leaves for a later task]"],
  "params": [
    { "id": "base", "label": "[Base delay]", "min": 50, "max": 1000, "step": 50, "default": 100, "unit": "ms" },
    { "id": "factor", "label": "[Growth factor]", "min": 1, "max": 4, "step": 0.5, "default": 2, "unit": "×" },
    { "id": "retries", "label": "[Max retries]", "min": 1, "max": 8, "step": 1, "default": 5, "unit": "" },
    { "id": "cap", "label": "[Delay cap]", "min": 500, "max": 30000, "step": 500, "default": 10000, "unit": "ms" }
  ],
  "toggles": [
    { "id": "fullJitter", "label": "[Toggle label — what the alternate mode changes]", "default": false }
  ],
  "facts": [
    { "text": "[Sourced fact the model illustrates]", "source": "E1" },
    { "text": "[Second sourced fact]", "source": "E2" }
  ],
  "simplifications": [
    "[what the model deliberately reduces]",
    "[second declared simplification]"
  ]
}
