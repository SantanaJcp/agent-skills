{
 "status": "approved",
 "stamp": "ACCEPTED",
 "acceptedVia": "chat 2026-07-23 — candidate pasted and confirmed after comparing all three rendered directions",
 "recordTitle": "Empty tag-filter result: Explain and suggest (direction B)",
 "summary": "With all three directions rendered against the identical five-skill dataset, direction B was accepted: it explains why the result is empty and turns the miss into navigation via real tag chips, at a bounded maintenance cost.",
 "canonicalPath": "decisions/tag-filter-empty-state.md",
 "decisions": [
  {
   "id": "empty-state-direction",
   "selection": "b",
   "rationale": "B is the only direction that gives the user somewhere to go; chips derive from visibleSkills() so they cannot drift from the real tag set.",
   "acceptedVia": "chat 2026-07-23 — explicit confirmation of the pasted candidate"
  }
 ],
 "exampleWorking": {
  "selected": "b",
  "rationale": "B is the only direction that gives the user somewhere to go; chips derive from visibleSkills() so they cannot drift from the real tag set."
 },
 "canonicalBody": "# Empty tag-filter result — accepted direction\n\nDirection B (Explain and suggest) is accepted for the empty tag-filter state.\nThe empty state names the missing tag, states the catalog's real tag set as\nchips that are live filters, and offers a clear-filter exit. Directions A\n(Quiet text) and C (Catalog stays visible) are preserved with their change\nconditions in the decision record."
}
